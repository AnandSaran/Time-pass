package com.gregantech.timepass.model

/**
 * Enum class for rail item type
 */
enum class RailItemDecorationTypeEnum(val value: Int) {
    /**
     * with
     */
    TYPE_RAIL_ITEM_DECORATION_ONE(0),

    /**
     * with poster, title, like, share, player, sound, loading
     */
    TYPE_RAIL_ITEM_DECORATION_TWO(1),
}